# 系统架构计划

## 🎯 最终技术栈

| 层级           | 技术                             | 说明                                     |
| -------------- | -------------------------------- | ---------------------------------------- |
| **MCP Server** | **Python** (`mcp` 库)            | 与 bot 环境一致                      |
| **API 后端**   | **Python FastAPI**               | 与 MCP Server 统一语言栈，异步高性能     |
| **数据库**     | **PostgreSQL**                   | 支持后续向量文档接入 (pgvector)          |
| **前端**       | **Vue 3 + Vite**                 | 组件化 SPA，现代化                       |
| **认证**       | **JWT (access + refresh token)** | OAuth2 标准流程，bcrypt 加密             |
| **邮件**       | **smtplib + QQ SMTP**            | 批量发送，JSON 文件存储邮件对象          |
| **LLM Bot**    | **大模型自主判断**               | 在 MCP 工具层增加意图判断，区分聊天/办事 |

---

## 📁 最终项目结构

```
Agent/
├── mcp-server/                    # MCP 服务器 (Python)
│   ├── pyproject.toml
│   ├── src/
│   │   ├── __init__.py
│   │   ├── server.py             # MCP Server 入口
│   │   ├── tools/
│   │   │   ├── __init__.py
│   │   │   ├── execute_command.py # 执行命令工具
│   │   │   ├── send_email.py      # 发送邮件工具
│   │   │   ├── get_results.py     # 获取命令结果
│   │   │   └── write_file.py      # 写文件工具（LLM 判断为办事时触发）
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── command_executor.py # 命令执行引擎（沙箱）
│   │   │   ├── email_service.py   # QQ SMTP 邮件服务
│   │   │   └── db.py             # PostgreSQL 数据库服务
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   └── schemas.py        # Pydantic 数据模型
│   │   └── config.py             # 配置管理
│   └── mcp-config.json           # MCP 配置（给 astr 用）
│
├── backend/                       # REST API 后端 (FastAPI)
│   ├── requirements.txt
│   ├── src/
│   │   ├── __init__.py
│   │   ├── main.py               # FastAPI 入口
│   │   ├── config.py             # 环境变量配置
│   │   ├── database.py           # PostgreSQL 连接池
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── user.py           # 用户模型 (SQLAlchemy)
│   │   │   ├── command.py        # 命令记录模型
│   │   │   └── email_record.py   # 邮件记录模型
│   │   ├── schemas/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py           # 登录/注册请求响应
│   │   │   ├── command.py        # 命令相关 schema
│   │   │   └── email.py          # 邮件相关 schema
│   │   ├── routes/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py           # /api/auth/*
│   │   │   ├── commands.py       # /api/commands/*
│   │   │   └── email.py          # /api/email/*
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── auth_service.py   # JWT 签发/验证、bcrypt
│   │   │   ├── command_service.py
│   │   │   └── email_service.py  # QQ SMTP 批量发送
│   │   └── middleware/
│   │       ├── __init__.py
│   │       └── auth.py           # JWT 鉴权中间件
│   └── alembic/                  # 数据库迁移
│
├── frontend/                      # Vue 3 前端
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router/
│       │   └── index.js          # Vue Router 路由守卫
│       ├── stores/
│       │   └── auth.js           # Pinia 认证状态管理
│       ├── api/
│       │   ├── index.js          # Axios 封装 + 拦截器
│       │   ├── auth.js           # 登录注册 API
│       │   ├── commands.js       # 命令结果 API
│       │   └── email.js          # 邮件 API
│       ├── views/
│       │   ├── LoginView.vue     # 登录页
│       │   ├── RegisterView.vue  # 注册页
│       │   ├── DashboardView.vue # 仪表盘（命令结果）
│       │   ├── CommandDetail.vue # 单条命令详情
│       │   └── EmailView.vue     # 邮件发送页（含批量）
│       ├── components/
│       │   ├── NavBar.vue
│       │   ├── CommandTable.vue
│       │   └── EmailForm.vue
│       └── assets/
│
├── data/                          # 数据存储
│   ├── email_objects/            # 邮件对象 JSON 文件（批量发送预留）
│   │   └── templates/            # 邮件模板
│   └── uploads/                  # 上传文件
│
├── docker-compose.yml            # 一键部署
└── .env.example                  # 环境变量模板
```

---

## 🧠 LLM Bot 意图判断流程

```
QQ 消息进入
    ↓
astr bot 将消息发送给 LLM（已有的大模型）
    ↓
LLM 判断意图：
    ├── "聊天" → 直接回复（不触发 MCP Tool）
    └── "办事" → 调用对应 MCP Tool：
        ├── 执行代码/命令 → execute_command
        ├── 生成文件/写代码 → write_file
        ├── 发邮件 → send_email
        └── 查结果 → get_results
```

MCP Server 提供 **Tool Description** 让 LLM 自己选择调用哪个工具，无需硬编码规则。

---

## 🗄️ 数据库表设计 (PostgreSQL)

```sql
-- 用户表
CREATE TABLE users (
    id            SERIAL PRIMARY KEY,
    username      VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    email         VARCHAR(100),
    is_active     BOOLEAN DEFAULT TRUE,
    created_at    TIMESTAMP DEFAULT NOW(),
    updated_at    TIMESTAMP DEFAULT NOW()
);

-- 命令记录表
CREATE TABLE commands (
    id            SERIAL PRIMARY KEY,
    user_id       INTEGER REFERENCES users(id),
    source        VARCHAR(20) DEFAULT 'qq',        -- 'qq' | 'web'
    command_text  TEXT NOT NULL,
    command_type  VARCHAR(30) DEFAULT 'shell',     -- 'shell' | 'python' | 'file_write'
    status        VARCHAR(20) DEFAULT 'pending',    -- pending/running/done/error
    result        TEXT,
    error_message TEXT,
    exit_code     INTEGER,
    environment   JSONB DEFAULT '{}',              -- 预留扩展
    created_at    TIMESTAMP DEFAULT NOW(),
    finished_at   TIMESTAMP
);

-- 邮件对象表（存储发送对象扩展信息）
CREATE TABLE email_objects (
    id            SERIAL PRIMARY KEY,
    user_id       INTEGER REFERENCES users(id),
    name          VARCHAR(100),                    -- 联系人姓名
    email_address VARCHAR(255) NOT NULL,
    company       VARCHAR(200),                    -- 公司
    position      VARCHAR(200),                    -- 职位
    tags          JSONB DEFAULT '[]',              -- 标签
    notes         TEXT,                            -- 备注
    metadata      JSONB DEFAULT '{}',              -- 扩展元数据
    is_active     BOOLEAN DEFAULT TRUE,
    created_at    TIMESTAMP DEFAULT NOW(),
    updated_at    TIMESTAMP DEFAULT NOW()
);

-- 邮件发送记录表
CREATE TABLE email_records (
    id            SERIAL PRIMARY KEY,
    user_id       INTEGER REFERENCES users(id),
    object_id     INTEGER REFERENCES email_objects(id), -- 关联邮件对象
    to_address    VARCHAR(255) NOT NULL,
    cc_address    TEXT,
    bcc_address   TEXT,
    subject       VARCHAR(500) NOT NULL,
    body          TEXT,
    body_html     TEXT,                            -- HTML 格式
    attachments   JSONB DEFAULT '[]',              -- 附件列表
    status        VARCHAR(20) DEFAULT 'pending',    -- pending/sent/failed
    error_message TEXT,
    sent_at       TIMESTAMP,
    created_at    TIMESTAMP DEFAULT NOW()
);

-- JWT Token 黑名单（登出 / refresh）
CREATE TABLE token_blacklist (
    id            SERIAL PRIMARY KEY,
    token_jti     VARCHAR(255) UNIQUE NOT NULL,
    expires_at    TIMESTAMP NOT NULL,
    created_at    TIMESTAMP DEFAULT NOW()
);
```

---

## 🔐 认证方案

采用 **OAuth2 Password Flow + JWT**：

```
注册：POST /api/auth/register
  → 验证 username/email 唯一性
  → bcrypt 加密密码
  → 返回用户信息（不含密码）

登录：POST /api/auth/login
  → 验证 username + password
  → 签发 access_token (30min) + refresh_token (7d)
  → 返回 token 对

刷新：POST /api/auth/refresh
  → 验证 refresh_token
  → 签发新的 token 对
  → 旧 refresh_token 加入黑名单

登出：POST /api/auth/logout
  → 将 access_token 的 jti 加入黑名单

前端：
  → Axios 拦截器自动附加 Authorization: Bearer xxx
  → 401 自动尝试 refresh
  → Vue Router 路由守卫检查登录态
```

---

## 📧 邮件服务设计

```
QQ SMTP 配置：
  - SMTP Server: smtp.qq.com
  - Port: 465 (SSL) / 587 (TLS)
  - 使用 QQ 邮箱授权码

邮件对象 JSON 文件存储（data/email_objects/）：
  - contacts.json：所有联系人
  - groups/：分组联系人列表
  - templates/：邮件模板

批量发送流程：
  1. 前端选择多个 email_object
  2. 后端遍历发送
  3. 每条结果记录到 email_records 表
  4. 支持断点续发（标记 pending/sent/failed）
```

---

## 🚀 部署方案 (Docker Compose)

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg16  # 已内置 pgvector
  backend:
    build: ./backend
    depends_on: [postgres]
  mcp-server:
    build: ./mcp-server
    depends_on: [postgres, backend]
  frontend:
    build: ./frontend
    ports: ["80:80"]   # nginx 静态服务
```

---

# 🏗️ MVP 版本分级计划

## V0 — 基础设施层 (P0) 🔴

> **目标**：搭好骨架，跑通最核心的链路。QQ → MCP → 执行命令 → 写入数据库。

### V0.1 — 项目脚手架 & 数据库

| 任务 | 说明 |
|------|------|
| 初始化 `mcp-server/` Python 项目 | `pyproject.toml`，安装 `mcp`、`asyncpg`、`psutil` 等依赖 |
| 初始化 `backend/` FastAPI 项目 | `requirements.txt`，FastAPI + SQLAlchemy + Alembic + bcrypt + PyJWT |
| 初始化 `frontend/` Vue 3 + Vite 项目 | `npm create vite@latest`，安装 Vue Router、Pinia、Axios |
| PostgreSQL 建表脚本 | 5 张核心表（users, commands, email_objects, email_records, token_blacklist） |
| Docker Compose | PostgreSQL + backend + mcp-server + frontend 四服务编排 |

### V0.2 — MCP Server 核心工具

| 任务 | 说明 |
|------|------|
| MCP Server 入口 `server.py` | 注册工具，连接 PostgreSQL |
| `execute_command` 工具 | 接收命令字符串，沙箱执行，结果写入数据库，返回给调用方 |
| `get_results` 工具 | 根据用户 ID 查询历史命令及结果 |
| `write_file` 工具 | LLM 判断"办事"时，将生成内容写入文件系统 |
| 数据库服务 `db.py` | asyncpg 连接池，CRUD 封装 |
| MCP 配置文件 `mcp-config.json` | 供 astr bot 加载 |

### V0.3 — 后端 API 核心

| 任务 | 说明 |
|------|------|
| `/api/auth/register` | 用户注册，bcrypt 加密 |
| `/api/auth/login` | 用户登录，签发 access_token + refresh_token |
| `/api/auth/refresh` | 刷新 token |
| `/api/auth/logout` | 登出，token 加入黑名单 |
| `/api/commands/` (GET) | 分页查询命令历史（需 JWT） |
| `/api/commands/{id}` (GET) | 单条命令详情 |
| JWT 鉴权中间件 | 拦截未认证请求，返回 401 |

### V0.4 — 前端最小可用

| 任务 | 说明 |
|------|------|
| 文件管理模块 | `FilesView.vue`，展示 MCP 生成的物理文件并支持下载 |
| 项目脚手架 | Vite + Vue Router + Pinia + Axios |
| 注册页面 `RegisterView.vue` | 表单：username + password + email |
| 登录页面 `LoginView.vue` | 表单：username + password |
| Token 管理 | Pinia store + Axios 拦截器 + 路由守卫 |
| 仪表盘 `DashboardView.vue` | 展示命令历史列表（表格分页） |
| 命令详情 `CommandDetail.vue` | 展示单条命令的完整结果 |

> **V0 交付标准**：用户从 QQ 发命令 → 后台执行 → 网页登录后能查看执行结果。

---

## V1 — 邮件 & 命令增强 (P1) 🟡

> **目标**：完善邮件发送能力，命令执行体验优化。

### V1.1 — 邮件功能

| 任务 | 说明 |
|------|------|
| QQ SMTP 邮件服务 | `email_service.py`，对接 smtp.qq.com:465 |
| MCP `send_email` 工具 | QQ 发送邮件指令 → 解析收件人/主题/内容 → 发送 |
| `/api/email/send` | 网页端发送邮件（单发 + 批量） |
| `/api/email/records` | 查询邮件发送历史 |
| `/api/email/objects` (CRUD) | 邮件对象管理（联系人增删改查） |
| 邮件对象 JSON 存储 | `data/email_objects/contacts.json`，支持分组 |
| 邮件页面 `EmailView.vue` | 选择收件人、填写内容、单发/批量发送 |

### V1.2 — 命令执行增强

| 任务 | 说明 |
|------|------|
| 命令白名单/黑名单 | 配置允许/禁止的命令模式 |
| 超时控制 | 命令执行超时自动 kill（默认 60s） |
| 输出截断 | 超大输出自动截断，防止数据库溢出 |
| 实时轮询 | 前端轮询命令状态（pending → running → done） |
| WebSocket 推送 | （可选）命令状态实时推送 |

---

## V2 — 扩展增强 (P2) 🟢

> **目标**：长期运营需要的功能，不影响核心链路。

### V2.1 — 邮件高级功能

| 任务 | 说明 |
|------|------|
| HTML 邮件模板 | 可视化选择模板，变量替换 |
| 邮件定时发送 | 设置发送时间，后台调度 |
| 附件上传 | 前端上传附件 → 后端存储 → 邮件附带 |
| 邮件统计面板 | 发送量/成功率/失败原因统计 |
| 邮件对象 Excel 导入 | 批量导入联系人 |

### V2.2 — 系统增强

| 任务 | 说明 |
|------|------|
| pgvector 向量文档 | `CREATE EXTENSION vector`，文档嵌入存储 |
| LLM 对话历史 | 将 QQ 聊天记录存入数据库，支持上下文 |
| 用户管理后台 | 管理员查看/禁用用户 |
| Rate Limiting | API 限流，防暴力破解 |
| 日志系统 | 结构化日志 + 日志查询页面 |
| 系统监控面板 | CPU/内存/磁盘使用率仪表盘 |
| 多用户隔离 | 不同 QQ 号绑定不同用户，数据隔离 |

### V2.3 — 部署 & DevOps

| 任务 | 说明 |
|------|------|
| CI/CD | GitHub Actions 自动构建 Docker 镜像 |
| HTTPS | Nginx + Let's Encrypt |
| 数据库备份 | 定时 pg_dump + 远程存储 |
| 健康检查 | `/health` 端点 + Docker healthcheck |
| 告警通知 | 服务异常时通过 QQ/邮件告警 |

---

## 📊 优先级总结

| 版本 | 范围 | 预计工期 | 核心交付 |
|------|------|-----------|-----------|
| **V0 (P0)** | 基础架构 + 核心链路 | 3-5 天 | QQ 发命令 → 网页看结果 |
| **V1 (P1)** | 邮件 + 命令增强 | 2-3 天 | 网页发邮件 + QQ 发邮件 |
| **V2 (P2)** | 扩展优化 | 按需 | 向量文档、监控、CI/CD |



## E.X

新增缓存页
