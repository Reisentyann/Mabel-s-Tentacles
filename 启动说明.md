# Agent 项目启动说明

本项目由多个微服务组成，包括前端（Vue 3）、后端（FastAPI）、MCP 服务器（Python）以及数据库（PostgreSQL）。通过 Docker Compose 可以轻松实现一键部署与运行。

## ⚙️ 前提条件
1. 系统已安装 **Docker** 和 **Docker Compose**。
2. **网络配置（针对国内用户特别注意）**：由于构建过程需要拉取 `python:3.11-slim`、`node:20-alpine`、`nginx:stable-alpine`、`pgvector/pgvector:pg16` 等国外镜像，请务必在 Docker Desktop 等客户端中配置好 **代理（Proxy）** 或者是 **镜像加速器（Registry mirrors）**，否则在 build 或 pull 阶段极易出现 `EOF` 或 `Head "https://registry-1.docker.io/..."` 超时报错。

---

## 🚀 方式一：一键启动（推荐）

在项目根目录下，执行以下命令：
```bash
docker-compose up -d --build
```
该命令会自动完成以下工作：
1. 启动 `agent_postgres` 容器，并运行 `database/init.sql` 自动建表。
2. 构建并启动 `agent_backend` 后端 API 服务。
3. 构建并启动 `agent_mcp_server` MCP 工具服务端。
4. 构建并启动 `agent_frontend` 前端 Vue 静态网页（借助 Nginx 服务）。

### 服务访问端口
所有的服务均映射了自定义端口到宿主机，以免与你本地的其他服务冲突：

- **前端管理界面**: [http://localhost:18080](http://localhost:18080) （使用浏览器访问，进入后可注册、登录、查看命令执行详细历史）
- **后端 API 服务**: [http://localhost:18000/docs](http://localhost:18000/docs) （FastAPI 的 Swagger 交互式接口文档）
- **PostgreSQL 数据库**: 宿主机端口 `54322` （可使用 Navicat/DBeaver 连接，用户名: `postgres`, 密码: `postgres`, 数据库名: `agent_db`）
- **MCP Server**: [内部交互]

停止与清理容器：
```bash
docker-compose down
```

---

## 💻 方式二：本地开发环境逐一启动 (Local Dev)

如果您希望在本地修改代码并热重载查看效果，您可以脱离 Docker 分别启动这几个服务。

### 1. 启动数据库
您可以在本地安装 PostgreSQL 或者仅使用 Docker 启动数据库：
```bash
docker-compose up -d postgres
```

### 2. 启动后端 (Backend)
进入 `backend` 目录，安装依赖并启动：
```bash
cd backend
pip install -r requirements.txt
# 设置环境变量指向数据库
set POSTGRES_USER=postgres
set POSTGRES_PASSWORD=postgres
set POSTGRES_HOST=localhost
set POSTGRES_PORT=54322
set POSTGRES_DB=agent_db
uvicorn src.main:app --reload --host 0.0.0.0 --port 18000
```

### 3. 启动 MCP Server
进入 `mcp-server` 目录，安装依赖并启动：
```bash
cd mcp-server
pip install -e .
# 同样需要设置上述数据库环境变量
python -m src.server
```
*(MCP Server 主要提供给 Bot 以 stdio 或者特定的 mcp 协议进行连接通讯，`mcp-config.json` 里定义了 astr bot 的接入方式)*

### 4. 启动前端 (Frontend)
进入 `frontend` 目录，安装依赖并运行开发服务器：
```bash
cd frontend
npm install
npm run dev
```
前端开发服务器一般会运行在 `http://localhost:5173`。

---

## 🛠️ 项目主要功能演示
1. **MCP 接入**: `mcp-server/mcp-config.json` 定义了外部 Agent（例如 astr bot 等）连接的配置。Bot 可以通过调用 `execute_command` 运行命令、`write_file` 写文件、`get_results` 查历史记录。
2. **前后端交互**: 用户在页面 `http://localhost:18080` 注册后，可以查看到 MCP Server 实际在系统内落库的指令运行状态和标准输出/错误输出结果（支持分页）。