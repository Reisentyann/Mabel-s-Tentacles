# Write File 技能交互分析报告

根据代码审查，`write_file` 技能允许大模型（调用者）在系统安全沙箱内生成并保存文件，该流程发生在 `mcp-server` 组件中。以下是服务器与调用者之间详细的交互机制：

## 1. 交互流程

1. **调用者（大语言模型/客户端）发起请求**：
   - 客户端通过 MCP 协议向服务端发起调用 `write_file` 工具的请求。
   - **请求参数**：
     - `file_path` (string): 期望保存的文件相对路径和名称。
     - `content` (string): 需要写入文件的具体文本内容。

2. **服务器层面的接收与分发 (`src/server.py`)**：
   - FastMCP 监听到 `write_file` 请求，提取 `file_path` 和 `content` 参数。
   - 调用内部核心的业务函数 `write_file_tool(file_path, content)`。

3. **核心业务逻辑与安全处理 (`src/tools/write_file.py`)**：
   - **内容体积限制**：服务器首先检查 `content` 编码后的体积，如果超过 `5MB`，立即拒绝写入。
   - **路径清洗**：剥离目标路径开头的 `/` 或 `\` 等绝对路径符号，防止其伪装成系统绝对路径。
   - **防盘符攻击**：检查路径中是否含有 `:` （如 `C:`），如果是则报错拦截。
   - **路径解析与防逃逸 (Directory Traversal)**：
     - 使用 `resolve()` 解析路径，并消除所有的 `..` 等上级目录跳转指令。
     - 服务器会比较最终的目标绝对路径，强校验该路径是否仍受限于服务端的 `BASE_DIR` （即项目容器中的 `data` 目录）。如果不属于 `data` 目录树（被 AI 逃逸出去了），服务器会主动拦截并报错。
   - **目录与文件创建**：通过校验后，若父目录不存在，服务器会自动创建它们。
   - **内容写入**：使用 UTF-8 编码将 `content` 写入到系统硬盘。

4. **服务器响应返回**：
   - 服务器将处理结果构建为一个包含 `success` 和 `message` 的字典，最终转化为 JSON 字符串返回给调用者：
     - **成功时**：`{"success": true, "message": "Successfully wrote to /app/data/..."}`。
     - **失败时**：返回对应的报错提示或安全警告，如体积超限、尝试越权访问目录或系统 IO 异常。

## 2. 交互安全特性总结

- 这个 `write_file` 工具是被**强制关禁闭 (Sandboxed)** 的。所有的文件读写只能在这个 Docker 容器的 `/app/data/` (即宿主机的 `./data/`) 目录下进行。调用者不能随心所欲地去修改操作系统的配置文件（如 `/etc/nginx` 或 `/app/frontend`）。
- **容错处理**：它没有直接把后端的报错栈返回给调用者，而是把错误信息进行了友好的 JSON 包装，这可以使大语言模型根据失败的 `message` 自行调整路径再次发起尝试。
