# 批量下载文件功能规划

> 目标：前端选中多个文件 → 打包压缩成 zip → 下载到本地。**压缩后原文件必须保留，不删除。**

## 一、需求

1. 用户在 Files 页选中多个文件（跨目录）。
2. 点击「批量下载」，后端把选中的文件打包成一个 zip。
3. 浏览器下载这个 zip 到本地。
4. **硬约束：压缩只读不删** —— 打包完成后 `data/` 目录下的原文件原封不动。

## 二、架构决策：压缩放后端，不放 MCP

- 批量下载属于**网页下载链路**（前端 → 后端 → 文件），不是 MCP 工具（MCP 工具是给大模型调用的）。
- 下载接口本来就在后端（`/api/files/download`），压缩作为它的批量版，顺理成章放后端（Python FastAPI）。
- Python 标准库 `zipfile` 现成，无需新依赖。
- MCP 服务器（Go）只负责「造文件」，不参与下载。

数据流：

```
前端选中多个 path
  → POST /api/files/download-zip  {paths: [...]}
  → 后端 zipfile 打包（读 data/ 目录文件，写 zip）
  → 返回 zip 流
  → 浏览器下载
  → data/ 原文件不变
```

## 三、后端设计

### 接口

```
POST /api/files/download-zip
请求体：{"paths": ["a.txt", "sub/b.txt"]}
响应：application/zip（Content-Disposition: attachment; filename="files.zip"）
```

### 实现要点

```python
import tempfile
import zipfile
from fastapi.responses import StreamingResponse

@router.post("/download-zip")
async def download_zip(req: ZipRequest, token: str = Query("")):
    if settings.ACCESS_TOKEN and not hmac.compare_digest(token, settings.ACCESS_TOKEN):
        raise HTTPException(status_code=403, detail="Invalid access token")

    with tempfile.SpooledTemporaryFile(max_size=10 * 1024 * 1024) as tmp:
        with zipfile.ZipFile(tmp, "w", zipfile.ZIP_DEFLATED) as zf:
            for p in req.paths:
                target = _safe_resolve(p)   # 复用现有五道路径安全防线
                if target.is_file():
                    zf.write(target, arcname=p)  # arcname 用相对路径，保持目录结构
        tmp.seek(0)
        return StreamingResponse(
            tmp,
            media_type="application/zip",
            headers={"Content-Disposition": 'attachment; filename="files.zip"'},
        )
```

### 关键点

1. **原文件保留（核心）**：`zipfile.write()` 只「读源文件 + 写 zip」，**不移动、不删除**源文件。全程没有 `os.remove` / `shutil.move`，天然满足约束。
2. **路径安全**：复用 `_safe_resolve()`（`lstrip` / 盘符 / `resolve` / `is_relative_to` / `is_file` 五道防线），批量接口不重新造轮子、不留越权口子。
3. **目录结构**：`arcname=p`（相对 `data/` 的路径），zip 解开后保留原有子目录层级。
4. **内存友好**：用 `SpooledTemporaryFile`（≤10MB 走内存，超过自动落盘到临时文件），避免「BytesIO 把整个 zip 塞内存」导致大文件 OOM。
5. **鉴权**：与单文件下载一致，校验 `ACCESS_TOKEN`（留空则不校验）。

### 需要新增

- `schemas/` 加 `ZipRequest`（`paths: list[str]`）。
- `files.py` 加 `download_zip` 路由。

## 四、前端设计

### FilesView 改动

- `FileTreeNode` 的**文件节点**加复选框（目录节点可加「全选子项」，第一版先只做文件级选择）。
- 顶部加「批量下载」按钮 + 已选数量，未选中时禁用。
- 选中状态跨递归组件共享（用 `provide/inject` 或简单地在 FilesView 维护一个 `selectedPaths` 数组，通过 props/emit 传递）。

### 下载方式

zip 已经压缩、体积可控，用 `axios POST + blob` 即可（不强制流式）：

```js
// api/files.js
export const downloadZip = (paths) =>
  api.post('/files/download-zip', { paths }, { responseType: 'blob' });
```

```js
// FilesView.vue
const handleBatchDownload = async () => {
  const res = await downloadZip(selectedPaths.value);
  downloadBlob(new Blob([res.data]), 'files.zip');
};
```

### 需要新增/改动

- `api/files.js` 加 `downloadZip`。
- `FileTreeNode.vue` 加复选框 + emit `toggle`。
- `FilesView.vue` 维护 `selectedPaths`，加批量下载按钮和逻辑。

## 五、验收标准

1. 选中 2+ 个文件（含不同子目录）→ 批量下载 → 得到 `files.zip`。
2. 解压 zip：文件齐全、目录结构正确。
3. **验证原文件保留**：下载后 `ls data/` 确认所有原文件仍在，内容不变。
4. 路径安全：请求里带 `../../etc/passwd` 被 403/400 拦截，不会打进 zip。

## 六、分步实施

| 步骤 | 内容 |
|------|------|
| 1 | 后端：`ZipRequest` schema + `download_zip` 接口（zipfile + 路径安全） |
| 2 | 前端：`api/files.js` 加 `downloadZip`；`FileTreeNode` 加复选框 |
| 3 | 前端：`FilesView` 维护选中态 + 批量下载按钮 |
| 4 | 联调测试：多文件压缩、目录结构、**原文件保留**、路径穿越拦截 |

---

### 备选（若未来压缩要放到 MCP 侧）

如需让**大模型**也能触发「打包文件」，可另在 Go MCP 加一个 `compress_files` 工具（`archive/zip` 打包到 `data/`，同样只读不删），与网页下载接口并存。两者数据源都是 `data/`，互不影响。但当前「网页批量下载」这个需求，压缩放后端是更直接、更轻的做法。
