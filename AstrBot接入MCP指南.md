# AstrBot 接入 MCP 服务端操作指南

AstrBot 是一款功能强大的聊天机器人框架，它支持通过 Model Context Protocol (MCP) 接入外部工具，从而赋予机器人操作服务器、写入文件等“特权”能力。

本项目提供了一个 MCP 服务端（`agent_mcp_server`），目前已全面升级为基于网络 HTTP 的 **SSE (Server-Sent Events)** 通信模式。相比于传统的 stdio 模式，SSE 模式跨机器、跨容器的兼容性极佳，极大简化了 AstrBot 的接入流程。

## 1. 确认服务状态

在接入之前，请确保本项目的 MCP 服务端已经正常启动。如果您使用 `docker-compose up -d` 启动了本项目，那么 MCP 服务默认暴露在宿主机的 `18001` 端口。

您可以在宿主机上通过命令行验证服务连通性：
```bash
curl http://127.0.0.1:18001/sse
```
如果命令正常连接，不报错（或有流式日志输出），说明服务运行正常。

## 2. AstrBot 接入配置

在 AstrBot 的 MCP 配置文件或后台管理界面的 MCP 插件配置中，添加如下 **SSE 类型** 的服务器节点：

### 配置示例 (JSON 格式)

```json
{
  "mcpServers": {
    "agent-mcp-server": {
      "type": "sse",
      "url": "http://127.0.0.1:18001/sse"
    }
  }
}
```

### 💡 重要网络配置注意事项：

- **同台机器，独立 Docker 运行**：如果 AstrBot 和本项目都在同一台物理机上，且都是独立的 Docker 容器运行，直接填 `127.0.0.1` 可能会指向 AstrBot 容器自己。此时，请将 `127.0.0.1` 替换为宿主机的**内网局域网 IP** (例如 `192.168.1.100`)，或者是 `host.docker.internal`。
- **加入相同 Docker 网络**：如果您将 AstrBot 容器配置到了本项目的默认网络（通常是 `agent_default`）中，请将 URL 更改为 MCP 容器的内部服务名和内部端口：`http://agent_mcp_server:8001/sse`。
- **跨机器运行**：如果您在其他服务器上运行 AstrBot，请确保本项目服务器防火墙已放行 `18001` 端口，并将 IP 地址替换为本项目所在服务器的**公网/局域网 IP**。

## 3. 可用能力列表

成功接入并启用后，AstrBot 搭载的大语言模型将自动获知并能直接调用以下三个强大的 MCP 工具（Tools）：

1. **`execute_command`**: 能够执行 Shell 命令（在 `agent_mcp_server` 容器环境中执行）。参数为 `command` (命令内容) 和 `user_id` (调用者标识)。
2. **`get_results`**: 根据 `user_id` 获取该用户之前执行命令的历史记录和结果日志。
3. **`write_file`**: 可以将大模型生成的长文本、代码，直接保存为服务器 `/app/data/` (映射到宿主机项目根目录的 `data/`) 文件夹下的物理文件。

## 4. 测试验证

配置完成并重启 AstrBot 后，您可以在聊天窗口中向机器人直接发送以下对话进行测试：

> "帮我执行一个 ls -al 命令，我的 user_id 填 1"

如果 AstrBot 回复了类似 "已经为您执行，结果如下：" 并附带了目录文件的列表信息，说明 MCP 服务已经与 AstrBot 圆满建立连接！此时，您也可以在本项目前端的 Dashboard（仪表盘）中看到这条新增的命令执行记录。

## 5. 常见问题排查：为什么曾经连接被拒绝？

如果您在之前配置正确的情况下仍然遇到 `Connection Refused` 或连接失败的情况，这通常是因为 **Docker 网络端口映射与服务绑定地址冲突** 所导致。

**原因分析：**
在旧版本中，MCP 服务端（FastMCP）默认将其内部服务绑定在了容器内部的环回地址 `127.0.0.1` 上。当您在 `docker-compose.yml` 中映射了 `18001:8001` 端口时，外部（例如宿主机或其他容器中的 AstrBot）的请求流量可以到达容器内部，但由于容器内部服务只接收来自本地 `127.0.0.1` 的请求，从而直接抛弃了从映射端口进来的非本地流量，导致 AstrBot 无法连接。

**解决方案：**
我们目前已经更新了本项目的 `mcp-server/src/server.py` 代码，强制将 FastMCP 的监听网络地址设置为了 `0.0.0.0`（允许接收所有网络接口的请求）：
```python
mcp = FastMCP("agent-mcp-server", host="0.0.0.0", port=port, ...)
```
确保这行代码生效并重启服务即可彻底解决连接问题。如果您在此前自己克隆修改过服务端代码，请务必核对并确保网络监听地址绑定在 `0.0.0.0`。





# AstrBot 接入 Docker 版 MCP 服务端全流程复盘

## 背景与痛点

- **服务 A (AstrBot)**：运行在 Docker 容器中，作为大模型聊天机器人前端。
- **服务 B (Mabel-s-Tentacles / MCP Server)**：运行在另一个 Docker 容器中，提供执行 Shell 命令、读写文件等特权能力，基于 **SSE (Server-Sent Events)** 协议通信。
- **痛点**：由于 Docker 默认的网络隔离机制，AstrBot 容器无法直接通过 `127.0.0.1` 访问宿主机上映射的 MCP 端口，直接连接会导致 `Connection Refused`。

------

## 🛠️ 第一阶段：网络诊断与排查

为了打通两个孤立的容器，我们首先需要摸清它们各自的网络归属。

1. **查看宿主机上的 Docker 网络列表**：

   ```
   bash
   
   docker network ls
   ```

   **输出结果发现关键网络：**

   - `mabel-s-tentacles_default`（MCP 服务端所在的专用桥接网络）
   - `qqbot_default`（AstrBot 所在的专用桥接网络）

2. **定位容器名称**： 通过 `docker ps` 获取了两个服务的容器名称：

   - 机器人容器名：`<AstrBot容器名>`（例如 `qqbot`）
   - MCP 服务端容器名：`<MCP容器名>`（例如 `agent_mcp_server`）

------

## 🌐 第二阶段：动态网络桥接（核心突破）

无需重启 AstrBot 容器，我们利用 Docker 的动态网络连接功能，**将 AstrBot 容器直接拉入 MCP 服务端的网络中**，使其具备“跨网穿透”能力。

1. **执行桥接命令**：

   ```
   bash
   
   docker network connect mabel-s-tentacles_default <AstrBot容器名>
   ```

   - **原理**：这一步让 AstrBot 容器同时拥有了两个网卡，既保留了它原本的网络，又加入了 MCP 的内部局域网。此时，AstrBot 容器内可以直接通过 MCP 的 **“容器名”** 作为域名进行内网通信。

------

## ⚙️ 第三阶段：配置 AstrBot 插件参数

在同一个 Docker 网络下，容器间通信**无需经过宿主机的端口映射转发（18001 端口）**，而是直接通过容器内部监听的默认端口（`8001`）进行超高速内网传输。

在 AstrBot 的 MCP 配置中，选择 **SSE 模板** 并填写如下配置：

```
json{
  "transport": "sse",
  "url": "http://<MCP容器名>:8001/sse",
  "headers": {},
  "timeout": 5,
  "sse_read_timeout": 300
}
```

> 💡 **为什么是 `8001` 而不是 `18001`？** 因为两端已经在同一个 Docker 局域网内，它们就像在同一个房间里的两台电脑，直接呼喊对方的名字（`<MCP容器名>`）和内部房门号（`8001`）即可，不需要再走到室外去绕宿主机的 `18001` 大门。

------

## 🧪 第四阶段：测试与验证

保存配置并重启 AstrBot 后，通过聊天通道向机器人发送指令：

> *“帮我执行一个 ls -al 命令，我的 user_id 填 1”*

1. **大模型收到指令**：识别到需要调用 `execute_command` 工具。
2. **发起请求**：AstrBot 向 `http://<MCP容器名>:8001/sse` 发送 SSE 请求。
3. **执行并返回**：MCP 服务端执行命令，通过网络流式返回结果。
4. **前端呈现**：机器人成功输出宿主机/容器内的目录文件列表，前端仪表盘同步出现执行记录。**连接圆满成功！**

------

### 💡 总结：这套方案的优势

- **高性能、低延迟**：容器间通过 Docker 内部虚拟网卡直连，绕过了宿主机的网络栈（NAT）转发。
- **安全性高**：外部防火墙无需暴露内部的 `8001` 端口，只对内网开放，防止了指令执行服务被恶意扫描或攻击。
- **配置简单**：后续如果迁移服务器，只要保持相同的 Compose 网络名，配置中的 `url` 无需做任何修改。
