{
  "mcpServers": {
    "agent-server": {
      "command": "python",
      "args": [
        "-m",
        "src.server"
      ],
      "env": {
        "POSTGRES_USER": "postgres",
        "POSTGRES_PASSWORD": "postgres",
        "POSTGRES_HOST": "localhost",
        "POSTGRES_PORT": "5432",
        "POSTGRES_DB": "postgres"
      }
    }
  }
}