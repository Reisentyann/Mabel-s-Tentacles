# 架构方案（最终版）

> 目标：**Go 后端合并 MCP + HTTP API（取代 Python 后端），藏在内网；前端独立，经 nginx 反代对外**。核心风险控制：MCP 的 `execute_command`（RCE）绝不暴露公网。

## 一、总体架构

```
用户（任意设备） ──► nginx（公网 80/443，唯一公网入口）
                       ├── /        → 前端静态文件（dist）
                       └── /api/*   → 反代 → Go 后端（Docker 内网 backend:8080）

AstrBot（云服务器 Docker 内）──► Go 后端 backend:8080
                                  ├── /sse、/message → MCP（SSE，内网）
                                  └── /api/*          → HTTP API

Go 后端共享 service + store → PostgreSQL + data/ 目录
```

**安全边界**：
- 公网只暴露 nginx 的 80/443。
- Go 后端（含 `execute_command` RCE、文件读写）**只在 Docker 内网**，不映射公网端口，公网到不了。
- AstrBot 与 Go 后端同机，走 Docker 内网连 MCP。
- nginx 反代 `/api`，`/sse` 不反代（MCP 不出公网）。

## 二、目录结构

```
mcp-server-go/                 # Go 后端（MCP + HTTP API）
├── cmd/server/main.go         # 组装 + 启动 + 优雅关闭
├── config.yml(.example)
├── internal/
│   ├── config/                # 配置（分块）
│   ├── logging/               # slog
│   ├── store/                 # 数据层：Store 接口 + pgx 实现
│   │   ├── store.go
│   │   ├── operations.go
│   │   └── commands.go
│   ├── service/               # 业务逻辑（按职责分文件）
│   │   ├── files.go           # SafeWrite/SafeList/SafeModify
│   │   ├── command.go         # ExecuteCommand
│   │   └── archive.go         # ZipFiles（批量压缩，只读不删）
│   ├── tools/                 # MCP 工具（一个功能一个子包）
│   │   ├── registry.go        # 注册表 + Deps
│   │   ├── all/all.go         # 集中 blank import
│   │   ├── writefile/ listdatafiles/ modifydatafile/
│   │   ├── executecommand/ getresults/ segmentedreply/
│   ├── api/                   # HTTP REST API
│   │   ├── router.go          # 路由 + 中间件链
│   │   ├── files.go           # /api/files、/download、/download-zip
│   │   ├── operations.go      # /api/operations
│   │   └── middleware.go      # 鉴权 + 请求日志
│   └── mcp/server.go          # MCP server 组装
└── go.mod

frontend/                      # 前端（独立，nginx serve，后续优化）
├── src/
├── nginx.conf                 # / → dist；/api → backend:8080
└── Dockerfile                 # 多阶段：pnpm build → nginx

database/init.sql
docker-compose.yml             # nginx + backend + postgres（3 服务）
```

## 三、核心设计决策

### 1. Go 后端合并 MCP + HTTP API，取代 Python

`mark3labs/mcp-go` 的 SSEServer 是 `net/http.Handler`，与 REST API 挂同一个 `http.Server`（内网 8080）。一个进程两个入口：

```
/ sse、/message    → MCP（AstrBot）
/ api/*            → HTTP API（前端经 nginx）
```

Python `backend/` 待 Go 全部接住后移 `旧内容/`。

### 2. registry + all.go（加工具不改注册逻辑）

```go
// internal/tools/registry.go
type Deps struct {
    Cfg   *config.Config
    Store store.Store           // 接口，非具体类型
}
type Registrar func(s *server.MCPServer, deps Deps)
var registrars []Registrar
func Register(r Registrar) { registrars = append(registrars, r) }
func RegisterAll(s *server.MCPServer, deps Deps) { for _, r := range registrars { r(s, deps) } }
```

```go
// internal/tools/all/all.go —— 加工具只改这一个文件的 import 列表
import (
    _ "…/writefile"
    _ "…/listdatafiles"
    // … 新增工具在此加一行
)
```

每个工具子包 `func init() { tools.Register(register) }`。**加工具 = 新建子包 + all.go 加一行 import**，注册逻辑零改动。HTTP API 层同样用 registry（api 包内集中注册路由）。

### 3. 接口注入（避免上帝对象 + 可测试）

`store.Store` 定义成 interface，工具/API 依赖接口，不依赖 `*pgxpool.Pool`。单元测试可 mock，未来换实现不动上层。

### 4. 鉴权：内网隔离为主，token 兜底

| 入口 | 保护方式 |
|------|----------|
| MCP `/sse` `/message` | **内网隔离（主）** + `mcp.api_key` Bearer（可选兜底） |
| HTTP `/api/*` | **nginx 反代 + 内网（主）** + `api.access_token`（可选兜底） |
| 下载 `?token=` | 流式下载带不了 header，用 query token |

- token 留空 = 关闭（内网隔离已兜底）；填了就启用。
- **token 不得写进前端 JS**（前端代码公开）。
- 与之前讨论一致：`execute_command` 是 RCE，其端点绝不暴露公网——由「backend 不映射公网端口 + nginx 不反代 /sse」保证。

### 5. service 按职责分文件

`files.go`（文件读写/列表）、`command.go`（命令执行）、`archive.go`（zip）。路径安全统一收敛在 `resolveWithin`，MCP 工具与 HTTP API 复用，不重复造。

### 6. store 返回业务错误，api 层映射 HTTP 状态

```go
var ErrNotFound = errors.New("not found")
// api 层：ErrNotFound → 404 {"detail":"not found"}；连接错误 → 500
```

响应体统一 `{"detail":"..."}`（与 FastAPI 一致，前端无感）。

### 7. HTTP 请求日志中间件

`method path status duration` 结构化日志；MCP 侧已有 `RecordOperation` 业务存档。

### 8. 优雅关闭处理 SSE 长连接

`Shutdown` 前关闭 MCP SSE 会话（`CloseSessions`），避免长连接卡死。`/health` 供 healthcheck。

## 四、批量下载（Go `archive/zip`）

```
POST /api/files/download-zip   body: {"paths":["a.txt","sub/b.txt"]}
响应：application/zip（Content-Disposition: attachment; filename="files.zip"）
```

```go
// service/archive.go
func ZipFiles(dataDir string, paths []string, w io.Writer) error {
    zw := zip.NewWriter(w)
    for _, p := range paths {
        target, err := resolveWithin(dataDir, p)   // 复用路径安全
        if err != nil || !isFile(target) { continue }
        f, _ := os.Open(target)
        zf, _ := zw.Create(p)                       // arcname 相对路径，保留目录结构
        io.Copy(zf, f)
        f.Close()
    }
    return zw.Close()
}
```

- **原文件保留**：全程 `os.Open` 读 + 写 zip，无 `os.Remove`/`os.Rename`，天然只读不删。
- 流式：直接写 HTTP 响应，不落临时 zip，内存/磁盘友好。
- 前端：`FileTreeNode` 文件节点加复选框（`provide/inject` 共享选中态）+ 顶部「批量下载」，`axios POST + blob` 下载。

## 五、配置分块

```yaml
server:
  port: 8080            # 内网端口（nginx 反代 /api，AstrBot 内网连 /sse）
  base_url: "http://backend:8080"
data_dir: "./data"

log:
  level: "info"
  format: "json"

database:
  url: ""
  host: "postgres"
  port: "5432"
  user: "postgres"
  password: "postgres"
  name: "agent_db"
  max_conns: 10

mcp:
  api_key: ""           # MCP 鉴权（Bearer），空=关闭（内网隔离已兜底）

api:
  access_token: ""      # HTTP 下载鉴权（query token），空=关闭
```

## 六、部署（docker-compose 3 服务）

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg16
    volumes: [postgres_data, ./database/init.sql]

  backend:              # Go 后端（MCP + API），不映射公网端口
    build: ./mcp-server-go
    environment: [DATABASE_URL=…, MCP_BASE_URL=http://backend:8080]
    volumes: [./data:/app/data]
    depends_on: [postgres]      # 无 ports：仅在 Docker 内网可达

  nginx:                # 唯一公网入口
    build: ./frontend           # 多阶段：pnpm build → nginx serve dist
    ports: ["80:80"]
    depends_on: [backend]
```

`frontend/nginx.conf`：

```nginx
location /          { root /usr/share/nginx/html; try_files $uri $uri/ /index.html; }
location /api/      { proxy_pass http://backend:8080; }   # 反代到内网 Go 后端
# 不反代 /sse，MCP 不出公网
```

## 七、测试策略

- `service`：`resolveWithin` 路径穿越（`../`、盘符、绝对路径）、`ExecuteCommand` 超时、`ZipFiles` 打包后源文件仍存在。
- `store`：接口 + 假实现（或真实 pgx + 测试库）。
- 开闭友好的前提是「有测试才敢扩展」，与 registry/接口注入配套。

## 八、实施步骤

| 步骤 | 内容 |
|------|------|
| 1 | 重构 `store.Store` 为接口 + 拆分 operations/commands 文件 |
| 2 | 重构 tools 为 registry + `all.go` + 接口注入 Deps |
| 3 | 新增 `service/archive.go`（ZipFiles）+ 测试 |
| 4 | 新增 `internal/api` 层（router/middleware/files/operations），挂进 mux |
| 5 | 配置分块 + `main.go` 组装 + 优雅关闭 + `/health` |
| 6 | 前端 `nginx.conf` 反代指向 Go 后端 + `docker-compose` 3 服务 |
| 7 | 联调：MCP 工具、文件下载、批量下载、存档、内网隔离 + 鉴权兜底 |
| 8 | Python `backend/` 退役（Go 接住后移 `旧内容/`） |
