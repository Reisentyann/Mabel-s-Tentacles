# 操作系统生成文件 (OS generated files)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 编辑器与 IDE (Editor directories and files)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python (Backend & MCP Server)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# 虚拟环境与环境变量 (Virtual Environments & Env files)
.env
.env.*

# 配置文件 (Config files - contains sensitive credentials)
backend/config.yml
!backend/config.yml.example
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Node.js / Vue / Vite (Frontend)
node_modules/
dist/
dist-ssr/
*.local
.npm

# 日志文件 (Logs)
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 数据库、持久化数据卷与临时测试文件 (Database, Data Volumes & Temp files)
data/
postgres_data/
test.py

# 缓存与测试覆盖率 (Cache & Coverage)
.pytest_cache/
.coverage
htmlcov/
.eslintcache

# 退役/清理内容（本地保留，不入库；旧代码已在 git 历史中）
旧内容/
旧文件/

# 测试夹具（含 .log 样本，豁免上方 *.log 规则）
!test/fixtures/**

# 夹具验证报告（运行产物，不入库）
test/fixtures-report.json

# 本地小结备忘（不入库）
小结.md
