# 部署指南（Docker Compose / 阿里云 Alinux）

本项目为 **Go 单体后端**（同一进程提供 MCP 服务与 HTTP API）+ **Vue 3 前端** + **PostgreSQL**，通过 `docker-compose.yml` 编排为三个容器。除前端（`18080`）外，所有端口均绑定在 `127.0.0.1`，不对公网暴露。

| 容器 | 宿主机端口 | 说明 |
|------|-----------|------|
| `agent_postgres` | `127.0.0.1:54322` | PostgreSQL 16（pgvector），仅本机可访问 |
| `agent_backend` | `127.0.0.1:18001` | Go 后端，容器内 `8080`，同端口提供 MCP（`/sse`）与 HTTP API（`/api`） |
| `agent_frontend` | `0.0.0.0:18080` | Nginx：托管前端静态文件，并将 `/api/` 反代给后端 |

## 1. 环境准备

确保云服务器已安装：

- **Docker** (>= 20.10)
- **Docker Compose** (>= 2.0，即 `docker compose` 插件)
- **Git**

若拉取镜像缓慢，建议在服务器的 `/etc/docker/daemon.json` 中配置阿里云 Docker 镜像加速器。

## 2. 克隆代码与配置环境变量

```bash
git clone git@github.com:Reisentyann/Mabel-s-Tentacles.git
cd Mabel-s-Tentacles
```

`docker-compose.yml` 中的变量均有默认值，不创建 `.env` 也能启动，但默认值不安全（数据库密码 `postgres`、管理员 `admin/admin123`、JWT 密钥为示例值），**生产环境必须覆盖**。在项目根目录创建 `.env`（Docker Compose 会自动读取）：

```dotenv
POSTGRES_USER=postgres
POSTGRES_PASSWORD=一个强数据库密码
POSTGRES_DB=agent_db
JWT_SECRET=用 openssl rand -hex 32 生成
ADMIN_USERNAME=admin
ADMIN_PASSWORD=一个强管理员密码
ACCESS_TOKEN=用于 /api/files/download 的下载令牌（可选）
```

后端启动时会自动检测示例默认密钥并打印告警日志，看到告警请回到这一步修改。

## 3. 服务启动

在项目根目录执行：

```bash
docker compose up -d --build
```

首次构建会编译 Go 后端并打包前端（Vite），可能需要几分钟。启动完成后：

- 数据库首次初始化会自动执行 `database/init.sql` 建表；
- 后端会自动创建默认管理员（`ADMIN_USERNAME` / `ADMIN_PASSWORD`）。

## 4. 网络安全与端口策略（阿里云安全组）

安全组**只需放行 `18080` (TCP)** 供 Web 用户访问。

- `54322`（数据库）、`18001`（MCP/后端）已硬性绑定 `127.0.0.1`，外部网络无法直接攻击这些服务；
- `18001` 仅供同机的 MCP 客户端（如 AstrBot）连接，接入方法见下一节。

若后续绑定域名并配置 HTTPS（或使用第 7 节的 1Panel 反向代理），则改为只放行 `80`/`443`，将 `18080` 收口到本机。

## 5. 连接 MCP 服务器（AstrBot 等 MCP 客户端）

MCP 服务与 HTTP API 由同一个 Go 进程提供，SSE 端点为 `/sse`（消息端点 `/message`），基于 HTTP 网络传输，跨机器、跨容器兼容性良好。

### 5.1 验证服务连通性

在宿主机上执行：

```bash
curl http://127.0.0.1:18001/sse
```

能正常连接并收到 SSE 事件流（不报 `Connection refused`）即说明服务运行正常。

### 5.2 客户端配置（SSE）

通用 MCP 客户端的 JSON 配置示例：

```json
{
  "mcpServers": {
    "agent-mcp-server": {
      "type": "sse",
      "url": "http://127.0.0.1:18001/sse"
    }
  }
}
```

### 5.3 按 AstrBot 的运行位置选择地址

| AstrBot 运行位置 | 填写的 URL | 说明 |
|---|---|---|
| 同一台宿主机、非容器运行 | `http://127.0.0.1:18001/sse` | 直接走宿主机回环端口映射 |
| 同机独立 Docker 容器 | `http://agent_backend:8080/sse` | 先把 AstrBot 容器接入本项目网络：`docker network connect mabel-s-tentacles_default <AstrBot容器名>`（网络名可用 `docker network ls` 确认）。同网络后直接用容器名 + 容器内端口 `8080`，不走宿主机 `18001` |
| 跨机器运行 | 不建议直接暴露公网 | `18001` 只绑定了 `127.0.0.1`。如确需跨机器访问，推荐用 SSH 隧道或 VPN 打通后按上述地址访问，并**务必**开启 5.4 的鉴权；不要把端口改成 `0.0.0.0` 裸奔——`execute_command` 工具可执行任意 Shell 命令 |

### 5.4 鉴权（可选，跨机器场景必开）

`docker-compose.yml` 默认没有把 `MCP_API_KEY` 传给后端，需要开启时，在 `backend` 服务的 `environment` 中增加一行后重新 `docker compose up -d --build`：

```yaml
      - MCP_API_KEY=${MCP_API_KEY:-}
```

并在 `.env` 中设置 `MCP_API_KEY=一串随机字符串`。之后客户端请求需携带请求头 `Authorization: Bearer <MCP_API_KEY>`：

```json
{
  "mcpServers": {
    "agent-mcp-server": {
      "type": "sse",
      "url": "http://127.0.0.1:18001/sse",
      "headers": { "Authorization": "Bearer <MCP_API_KEY>" }
    }
  }
}
```

### 5.5 可用工具

接入成功后，客户端（大模型）可调用以下 MCP 工具：

`write_file` / `read_file` / `list_data_files` / `modify_data_file` / `copy_file` / `describe_file` / `execute_command` / `get_results`

文件类工具均落在项目根目录的 `data/`（映射到容器内 `/app/data`），有防目录穿越校验与 5MB 大小上限；`execute_command` 执行 Shell 命令（60s 超时，输出截断 256KB）。

### 5.6 测试验证

保存配置并重启 AstrBot 后，在聊天窗口发送：

> 帮我执行一个 ls -al 命令，user_id 填 1

机器人返回目录文件列表，且 Web 前端 Dashboard 中同步出现这条命令执行记录，即代表 MCP 接入成功。

## 6. 验证 Web 部署

浏览器访问：

```
http://<服务器公网IP>:18080/
```

能正常打开页面并成功注册/登录（无 Network Error / 502），即部署完成。

## 7. （可选）通过 1Panel 挂载到子域名

推荐使用 1Panel 的**反向代理**模式（而非静态网页模式），可直接复用本项目前端容器内置的 Nginx（静态资源 + `/api/` 反代一体），保持容器化架构完整性：

1. **域名解析**：在域名服务商处添加 A 记录，将子域名（例如 `mabel.example.com`）解析到服务器公网 IP。
2. **创建反代网站**：1Panel → 网站 → 创建网站 → 选择「反向代理」：
   - 主域名：`mabel.example.com`
   - 代理地址：`http://127.0.0.1:18080`
3. **配置 HTTPS**：进入该网站配置 → HTTPS → 启用（可用 Let's Encrypt 申请免费证书），并开启「HTTP 严格重定向」强制跳转。
4. **收紧安全组**：改为只放行 `80`/`443`；`18080`、`18001`、`54322` 一律不放行。外网流量统一由 1Panel 接管，无法绕过它探测后端敏感端口。

## 8. 后续维护

查看报错日志：

```bash
docker logs agent_backend --tail 100 -f
docker logs agent_frontend --tail 100 -f
```

重置数据库（危险操作，会清除所有数据）：

```bash
docker compose down -v
docker compose up -d
```
