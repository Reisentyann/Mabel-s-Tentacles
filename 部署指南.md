# Agent 线上部署指南 (Aliyun / Alinux)

在进行线上部署（特别是暴露到公网）前，本项目已经完成了底层重构，切断了高危端口的外网访问，并改用 Nginx 代理请求。请按照以下步骤在阿里云服务器上进行安全部署。

## 1. 环境准备
确保您的 Alinux 云服务器已安装以下基础环境：
- **Docker** (>= 20.10)
- **Docker Compose** (>= 2.0 / `docker compose` plugin)
- **Git**

*(注：代码仓库中的 Dockerfile 已默认配置了阿里云 PyPI 镜像源和淘宝 NPM 镜像源以加速构建。若拉取基础镜像缓慢，建议您在服务器的 `/etc/docker/daemon.json` 中配置阿里云的 Docker 镜像加速器。)*

## 2. 克隆代码与配置环境变量
1. 将项目代码 clone 或上传到云服务器。
2. 进入项目根目录：
   ```bash
   cd /path/to/Agent
   ```
3. 基于模板创建环境变量文件：
   ```bash
   cp .env.example .env
   ```
4. **修改 `.env` 配置文件 (极重要)**：
   打开 `.env` 文件，必须修改默认密码和密钥以保证安全。
   - `POSTGRES_PASSWORD`: 设置一个强数据库密码。
   - `JWT_SECRET`: 必须更改！可以用命令 `openssl rand -hex 32` 生成一个随机字符串填入。
   - `CORS_ORIGINS`: 如果前端和后端共享同一个域名（通过 Nginx 代理），此处可留空。如果前后端域名不同，请填入前端的公网域名。

## 3. 服务启动
在项目根目录执行以下命令，后台拉起所有服务：
```bash
docker-compose up -d --build
```
*首次构建包含编译前端资源（Vite）和安装 Python 依赖，可能需要几分钟。*

## 4. 网络安全与端口策略 (阿里云安全组配置)
我们已经在 `docker-compose.yml` 中做出了安全限制：
- 数据库 (`54322`)、后端 (`18000`)、MCP 服务 (`18001`) 现已被硬性绑定到 `127.0.0.1`，**外部网络无法直接通过这些端口攻击服务**。
- 前端 Nginx 服务暴露在端口 `18080`，并且 Nginx 内部已经配置了 `/api/` 路由自动将请求反向代理转发给后端的 FastAPI。

**在阿里云控制台的安全组设置中，您只需开放以下端口：**
- `18080` (TCP) - 允许 Web 用户访问前端页面。

如果您后续绑定了域名并配置了 HTTPS/443（或者在外层再套一个 Nginx 进行 SSL 卸载），则只需开放 80/443 端口，并将 `18080` 收口到本机即可。

## 5. 验证部署
部署完成后，直接在浏览器中访问您的服务器 IP：
```
http://<您的阿里云_公网IP>:18080/
```
您可以尝试注册一个新账号。由于前端接口已经改为相对路径 `/api` 并经过代理，跨域和网络连通性问题均已解决。

## 6. 后续维护
如果需要查看报错日志：
```bash
docker logs agent_backend --tail 100 -f
docker logs agent_frontend --tail 100 -f
```
如果要重置数据库（危险操作，会清除所有数据）：
```bash
docker-compose down -v
docker-compose up -d
```
