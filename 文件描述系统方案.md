# 文件描述系统方案（v2）

> 目标：给每个文件挂「可检索的描述/标签/属性」，支持按「创建者 / 类型 / 颜色」等条件找文件；描述可自动、可手动；支持复制；记录完整生命周期（含软删除）供审计。

## 一、需求回顾

1. 检索：找到「xxx 用户创建的 xxx 类型文件」「颜色是 xxx 的图片」等。
2. 描述来源：自动预处理 + 手动描述。
3. 复制：复制某人的文件（内容 + 描述）。
4. 审计：保留 `session_id` + `user_id`，事后能做安全审查。
5. 生命周期：记录创建时间、是否被删除（软删除），不物理丢元数据。

## 二、字段设计（完整）

每个文件一条记录，`file_path` 唯一标识（相对 `data/` 目录）。

```sql
CREATE TABLE file_metadata (
    -- 标识
    id          BIGSERIAL PRIMARY KEY,
    file_path   TEXT UNIQUE NOT NULL,      -- 相对 data 目录路径，唯一键

    -- 描述内容
    description TEXT,                      -- 自由文本描述
    tags        TEXT[] DEFAULT '{}',       -- 标签数组

    -- 类型
    file_type   TEXT,                      -- image / text / code / video
    mime_type   TEXT,                      -- image/png 等
    size_bytes  BIGINT,                    -- 文件大小（入表，便于排序/展示）

    -- 归属（两个都存，用于安全审查）
    session_id  TEXT,                      -- MCP 会话标识（自动归组）
    user_id     TEXT,                      -- 用户标识（显式传参）

    -- 扩展属性（灵活键值，见第三节说明）
    attributes  JSONB DEFAULT '{}',        -- color、width、作者、地点等任意键值

    -- 复制关系
    copied_from TEXT,                      -- 复制来源路径（可空）

    -- 生命周期
    is_deleted  BOOLEAN DEFAULT FALSE,     -- 软删除标志：是否已被删除
    deleted_at  TIMESTAMPTZ,               -- 删除时间（可空）
    created_at  TIMESTAMPTZ DEFAULT NOW(), -- 创建时间
    updated_at  TIMESTAMPTZ DEFAULT NOW()  -- 最后更新时间
);

CREATE INDEX idx_meta_tags  ON file_metadata USING GIN (tags);
CREATE INDEX idx_meta_attrs ON file_metadata USING GIN (attributes);
CREATE INDEX idx_meta_type  ON file_metadata (file_type);
CREATE INDEX idx_meta_by    ON file_metadata (user_id);
CREATE INDEX idx_meta_del   ON file_metadata (is_deleted);
```

**字段分类表**：

| 分类 | 字段 | 用途 |
|------|------|------|
| 标识 | `id`、`file_path` | 主键 + 文件定位 |
| 描述 | `description`、`tags` | 自由描述 + 标签 |
| 类型 | `file_type`、`mime_type`、`size_bytes` | 类型/大小（检索 + 展示） |
| 归属 | `session_id`、`user_id` | 谁、在哪个会话创建的（审计） |
| 扩展 | `attributes` | 颜色等任意属性 |
| 复制 | `copied_from` | 复制来源 |
| 生命周期 | `is_deleted`、`deleted_at`、`created_at`、`updated_at` | 软删除 + 时间戳 |

## 三、关于 JSONB 存储：为什么不每个属性建列

你担心「JSON 存进数据库难找」，这个顾虑我说明一下取舍：

**为什么 `attributes` 用 JSONB，而不是把 `color`、`width` 都建列？**
- 属性是**开放式、不可穷举**的：颜色、尺寸、作者、地点、语言、分辨率…… 每个都建列，表会无限膨胀，且每加一个属性都要 `ALTER TABLE`。
- 固定列只装得下「你此刻想得到的字段」，装不下「以后冒出来的字段」。

**JSONB 真的难找吗？其实不难** —— PostgreSQL 的 JSONB 不是把 JSON 当纯文本存：
- 它被解析成二进制结构，**有 GIN 索引**，`attributes @> '{"color":"red"}'` 走索引，和普通列一样快。
- 有完整查询语法：`->>'color'` 取值、`@>` 包含、`?` 查键存在。
- pgAdmin / DBeaver 里能展开成树查看，不是黑盒。

**分工原则（关键）**：
- **高频、固定、要索引/约束的字段 → 明确列**（file_type、user_id、is_deleted 这些）。
- **低频、灵活、开放式的属性 → `attributes` JSONB**（颜色等）。

这样既清晰（常用字段一目了然），又灵活（罕见属性不用改表）。如果你仍倾向「颜色等也建成列」，代价是以后每加一种属性都要改表 + 改代码，我可以改成 EAV 表（`file_attr(file_path, key, value)` 一行一属性），但那种查询更慢、更啰嗦，不推荐。

## 四、描述来源：自动 + 手动

1. **自动预处理**（`write_file` 落盘时）：推断 `file_type`/`mime_type`（扩展名）、记录 `size_bytes`、`session_id`/`user_id`。
2. **手动描述**（`describe_file`）：补充 `description`/`tags`/`attributes`。
3. 两者 upsert 到同一条记录（不存在建、存在更新）。AI 增强（图片颜色识别等）留到第二阶段，做成可插拔的「描述生成器」。

## 五、检索设计

```
GET /api/files/search
  ?q=关键词          → description / file_path 模糊匹配
  &tag=报告&tag=红色  → 标签（AND 包含）
  &type=image         → 文件类型
  &creator=xxx        → 创建者（user_id）
  &color=red          → 属性（attributes @> {"color":"red"}）
  &deleted=true       → 是否含已删除（默认 false，只看可用文件）
  &page=1&size=20
```

核心 SQL（默认过滤 `is_deleted = false`）：

```sql
SELECT * FROM file_metadata
WHERE is_deleted = $deleted                        -- 默认只看未删除
  AND ($1::text  IS NULL OR description ILIKE '%'||$1||'%' OR file_path ILIKE '%'||$1||'%')
  AND ($2::text[] IS NULL OR tags @> $2)
  AND ($3::text   IS NULL OR file_type = $3)
  AND ($4::text   IS NULL OR user_id = $4)
  AND ($5::jsonb  IS NULL OR attributes @> $5)
ORDER BY updated_at DESC
LIMIT … OFFSET …;
```

## 六、文件复制

```
POST /api/files/copy   body: {"source":"a.png", "target":"sub/a-copy.png"}
```

1. 复制文件内容（读源写目标，复用 `service` 安全路径逻辑）。
2. 复制元数据：`description`/`tags`/`file_type`/`attributes` 继承；`copied_from` 记源路径；`session_id`/`user_id` 记当前操作者（不是源文件的）。
3. 源文件与源元数据不变（只读不删）。

## 七、软删除（「是否可用」）

- 文件被删时**不物理删元数据**，只 `is_deleted=true` + `deleted_at=now()`。
- 检索默认过滤掉已删除的；`deleted=true` 时可查历史（审计/恢复）。
- 呼应项目的零删除偏好：元数据永远可追溯。

## 八、架构位置（融入 Go 后端）

| 层 | 新增 |
|----|------|
| `store` | `metadata.go`：表迁移 + `UpsertMetadata`/`GetMetadata`/`SearchFiles`/`CopyMetadata`/`SoftDelete` |
| `service` | `metadata.go`（描述/检索/复制业务）、`archive.go`（已有） |
| MCP 工具 | `describe_file`、`copy_file`、`search_files`（可选） |
| HTTP API | `GET /api/files/search`、`GET/PUT /api/files/{path}/metadata`、`POST /api/files/copy` |

## 九、分阶段实施

| 阶段 | 内容 | 依赖 |
|------|------|------|
| **P0** | `file_metadata` 表 + store CRUD + 检索接口 + `describe_file`/`copy_file` 工具 + `write_file` 自动记元数据 | store 接口化（架构方案步骤 1） |
| **P1** | 前端：搜索框 + 文件卡片展示描述/标签 + 复制按钮 | P0 |
| **P2** | 自动预处理增强：图片颜色/内容识别、文本摘要（插拔式描述生成器） | P0 |

---

## 还需你确认

1. **`user_id` 的类型**：`TEXT` 还是 `BIGINT`（关联 users 表）？现在 MCP 的 `execute_command` 用的是整数 `user_id`，但「xxx 用户」也可能是 QQ 号/昵称之类的字符串，我倾向 `TEXT`（宽松），你定。
2. **检索默认行为**：`deleted` 默认只查未删除的（`is_deleted=false`），对不对？
3. **属性过滤**：保持通用 `attributes @>`（灵活）可以吗？
