# Admin Account Configuration
ADMIN_USERNAME=Reisentyan
ADMIN_PASSWORD=Reisentyandaisuki666666.

# Backend & Database Configuration
POSTGRES_USER=agent_user
POSTGRES_PASSWORD=Reisentyandaisuki666666.PostGreSQL
POSTGRES_DB=agent_db

# JWT Authentication
# Generate a secure key using: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=Reisentyandaisuki666666.JWTSECRETpassWorld

# Email Configuration (Optional, for MCP Server)
SMTP_USER=reisentyan@qq.com
SMTP_PASSWORD=Reisentyandaisuki666666.

# CORS Configuration
# Comma-separated list of allowed origins. Leave empty if proxying via Nginx on the same domain.
CORS_ORIGINS=http://your-domain.com,https://your-domain.com
