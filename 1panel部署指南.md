# 1Panel 面板部署指南

当您在云服务器上使用 1Panel 面板时，如果希望将本项目的页面挂载到您的子域名上，本指南将为您提供详细步骤和方案建议。

## 方案选择：反向代理 vs 静态网页

**结论：强烈推荐使用“反向代理（Reverse Proxy）”**

### 为什么选择反向代理而不是静态网页？

1. **架构完整性（开箱即用）**：
   本项目采用 `docker-compose` 编排。项目内部的 `frontend` 服务不仅仅是一个存放静态文件的容器，它内部包含了一个配置好的 Nginx，用于**同时**提供静态网页服务和转发 `/api/` 路由请求给隔离在内网的 `backend` 容器。使用反向代理可以完美复用这一套现成的逻辑，保持容器化架构的一致性。
2. **避免配置繁琐**：
   如果选择 1Panel 的“静态网页”部署模式，您需要手动将项目编译后的前端打包文件（`dist`）上传到 1Panel 创建的网站目录中。除此之外，您还必须在 1Panel 的 Nginx 配置文件中手动编写反向代理规则，把前端网页请求的 `/api/` 指向后端的 `18000` 端口。这不仅繁琐，而且破坏了原有 `docker-compose` 自动化管理的便利性。
3. **安全与隔离**：
   通过 1Panel 的反向代理，外网流量统一由 1Panel 的主 Web 服务器（OpenResty/Nginx）接管（处理 HTTPS、拦截恶意请求等），然后再安全地转发到服务器本地的 `127.0.0.1:18080`（本项目前端容器映射的端口）。整个项目的微服务依然完全运行在 Docker 隔离环境中，提高了系统的安全性。

---

## 详细部署步骤

以下步骤假设您已经在云服务器的项目目录下，通过 `docker-compose up -d --build` 正常启动了所有服务。
（根据项目配置，前端默认占用本机的 `18080` 端口，后端占用 `18000` 端口，并且除前端外均未对外开放访问）。

### 第一步：准备子域名解析

在您的域名提供商（如阿里云、腾讯云、Cloudflare 等）处，添加一条 **A 记录**，将您的子域名（例如 `agent.yourdomain.com`）解析到您当前云服务器的公网 IP。

### 第二步：在 1Panel 中创建反向代理网站

1. 登录 1Panel 面板后台。
2. 在左侧菜单栏中选择 **“网站”** -> **“网站”**。
3. 点击右侧的 **“创建网站”** 按钮。
4. 在弹出的窗口顶部，选择 **“反向代理”** 选项卡。

### 第三步：填写反向代理配置

按照以下信息填写网站的创建配置：

- **主域名**：填写您的子域名（例如 `agent.yourdomain.com`）。
- **代号**：系统会自动生成，您也可以随意自定义，例如 `agent-web`。
- **代理名称**：填写一个便于识别的名字，例如 `agent-frontend`。
- **代理地址**：填写 **`http://127.0.0.1:18080`**。
  *(解析：1Panel 会将该域名的所有访问请求，转发给本地运行在 18080 端口的 Docker 容器。)*
- **备注**：Agent 项目前端反向代理。

检查无误后，点击右下角的 **“确认”**，等待网站创建成功。

### 第四步：配置 HTTPS 证书（强烈推荐）

现代 Web 应用通常都需要 HTTPS 来保证数据传输的安全。

1. 在 1Panel 网站列表中，找到刚刚创建的反代网站，点击 **“配置”**（或点击网站名）。
2. 在左侧导航栏点击 **“HTTPS”**。
3. 勾选 **“启用 HTTPS”**。
4. 如果您已经在 1Panel 配置了 Let's Encrypt 的 Acme 账号并申请了证书，在“选择证书”下拉框中选择对应证书即可；如果没有，您可以点击“申请证书”快速为该域名申请一个免费的 Let's Encrypt 证书。
5. 开启底部的 **“HTTP 严格重定向”** 开关（作用是将访客的 HTTP 请求自动强制跳转为 HTTPS）。
6. 点击 **“保存”**。

### 第五步：收紧服务器安全组策略

由于 1Panel 的 Nginx 已经接管了公网的访问请求，此时不需要在外网暴露 18080 端口了。
请进入您的云服务商（如阿里云/腾讯云）后台的 **安全组/防火墙** 设置：

1. **允许（放行）**：`80` 端口 和 `443` 端口（供网页正常访问）。
2. **禁止（或不要放行）**：项目的 `18080`、`18000`、`18001` 端口，以及数据库的 `54322` 端口。

这样一来，外部人员只能通过您的域名走 80/443 端口访问，无法直接绕过 1Panel 去探测后端的敏感端口。

### 第六步：验证部署

在浏览器地址栏输入您的子域名（如 `https://agent.yourdomain.com`）并回车。
如果您能正常看到项目的前端登录/主页面，并且在进行注册、登录等操作时提示成功（没有出现 Network Error 或 502 错误），则代表您的项目已经成功通过 1Panel 部署挂载上线！