# 报告：用户注册数据流向与前端注册失败排查

## 1. 注册数据流向分析
当用户在前端尝试注册新账号时，数据经历了以下完整的流向，最终持久化到数据库中：

1. **前端数据收集 (Frontend Data Collection)**
   - 用户在注册页面 (`frontend/src/views/RegisterView.vue`) 的表单中填写 `username`、`email`（可选）和 `password`，点击“Register”按钮后，触发绑定的 `handleRegister` 方法。
   - `handleRegister` 将用户输入的响应式数据 (`ref`) 提取并组装成一个数据对象 `data`。

2. **状态管理与 API 发起 (State Management & API Call)**
   - `handleRegister` 调用 Pinia 状态管理 (`frontend/src/stores/auth.js`) 中的 `registerAction(data)` 方法。
   - 该 Action 调用 `frontend/src/api/auth.js` 中封装的 `register` API。
   - 底层使用 Axios 实例 (`frontend/src/api/index.js`) 构建 HTTP `POST` 请求，将包含注册信息的 JSON 数据发送至后端接口（默认配置为 `http://localhost:8000/api/auth/register`）。

3. **后端路由接收与验证 (Backend Route & Validation)**
   - 请求到达 FastAPI 后端，由 `backend/src/routes/auth.py` 中的 `@router.post("/register")` 端点处理。
   - FastAPI 会根据 Pydantic 模型 `UserCreate` (`backend/src/schemas/auth.py`) 对传入的 JSON 请求体进行自动解析和数据验证，确保提供了必需的 `username` 和 `password`。

4. **业务逻辑与数据库持久化 (Business Logic & Database)**
   - 后端首先通过异步数据库会话 (`db`) 检查是否存在重名用户，如果存在则直接抛出 400 异常。
   - 若无重名，后端会调用 `backend/src/services/auth_service.py` 中的 `get_password_hash` 方法（基于 `passlib` 和 `bcrypt`），对用户明文密码进行安全的哈希加密。
   - 创建一个新的 SQLAlchemy ORM 模型实例 `User` (`backend/src/models/__init__.py`)，将加密后的密码和其它信息赋给它。
   - 调用 `db.add(db_user)` 将其加入数据库会话，随后 `await db.commit()` 将事务提交，此时数据正式流入 PostgreSQL 数据库的 `users` 表。
   - 最后执行 `await db.refresh(db_user)` 取回数据库在插入时自动生成的字段（如主键 `id` 和 `created_at` 时间戳），将此实例通过 `UserResponse` 模型序列化后响应给前端。

5. **前端后处理 (Frontend Post-processing)**
   - 注册请求成功后，前端代码会利用用户刚输入的账号密码自动调用 `loginAction` 完成登录，并利用 Vue Router 跳转到控制台 `/dashboard`。


## 2. 前端注册失败原因排查

根据对前后端代码的仔细排查，前端注册账号失败（并显示默认的 "Registration failed" 错误）是由以下**两个主要原因**叠加导致的：

### 原因一：前端 API 基础 URL 端口与后端暴露端口不一致
经过排查 `docker-compose.yml` 发现，后端服务暴露在宿主机的端口为 `18000`：
```yaml
ports:
  - "18000:8000"
```
然而在前端 `frontend/src/api/index.js` 中，Axios 的默认 `baseURL` 被硬编码成了 `http://localhost:8000/api`。
这就导致前端浏览器直接把请求发给了 8000 端口，从而报了网络连接失败或跨域错误。

### 原因二：后端的跨域（CORS）配置错误
即使修复了端口号，后端的跨域配置也存在阻碍。根据 Web 安全规范以及 FastAPI 的设计：
当 `allow_credentials=True`（允许携带 Cookie 等凭证信息）时，`allow_origins` **绝对不能被配置为通配符 `["*"]`**。这会导致 FastAPI 抛出严重断言错误：
> `AssertionError: Cannot use allow_origins=['*'] with allow_credentials=True`

**问题根源在 `backend/src/main.py`：**
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 允许所有源
    allow_credentials=True, # 允许携带凭证
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**详细原因：**
根据 Web 安全规范以及 FastAPI（底层基于 Starlette 框架）的设计：
当 `allow_credentials=True`（允许前端跨域请求携带 Cookie 等凭证信息）时，`allow_origins` **绝对不能被配置为通配符 `["*"]`**。必须明确指定允许请求的域名（如 `["http://localhost:5173"]`）。
因为存在这个违规配置，FastAPI 服务在启动或接收到跨域预检请求时，会直接抛出以下严重断言错误：
> `AssertionError: Cannot use allow_origins=['*'] with allow_credentials=True`

**导致前端的表现：**
由于后端服务要么崩溃、要么在跨域协商（Preflight OPTIONS 请求）阶段直接失败导致网络错误（Network Error），前端通过 Axios 发出的注册 POST 请求无法收到预期的 HTTP 响应。
此时 `err.response` 为 `undefined`，代码落入 `catch` 块中：
```javascript
} catch (err) {
  error.value = err.response?.data?.detail || 'Registration failed';
}
```
因为无法获取到详细错误响应结构，页面上最终就会显示后备信息：**"Registration failed"**。

**修复与修改操作：**
已采用推荐方案，明确指定前端所在的域名，并考虑到了用户环境中 Vue 前端运行在不寻常的 18080 端口。因此，后端 `backend/src/main.py` 已被修改为如下配置：
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:18080",
        "http://127.0.0.1:18080",
        "http://localhost:5173",  # default vite port fallback
        "http://127.0.0.1:5173",
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 原因三：数据库 `users` 表不存在 (Internal Server Error)
在修复了端口和 CORS 之后，注册请求到达了后端。但是由于后端报错 `500 Internal Server Error`，前端仍然将其抽象地显示为 Network Error。
通过在后端代码中添加调试日志并查看 `docker logs agent_backend`，定位到真正的报错信息：
> `asyncpg.exceptions.UndefinedTableError: relation "users" does not exist`

这说明 PostgreSQL 数据库中缺失 `users` 表。虽然项目提供了 `database/init.sql` 脚本，但由于 Docker 容器挂载了持久化数据卷（`postgres_data`），在最初启动失败或未正确初始化的情况下，数据库标记已经初始化，导致后续哪怕重构镜像也不会再次执行 `init.sql` 脚本建表。

**修复操作：**
通过执行以下命令强制删除挂载的旧数据卷，让 PostgreSQL 在下一次启动时重新读取 `init.sql` 并在空白数据库中完整建表。
```bash
docker-compose down -v
docker-compose up -d
```
现在所有的表都已经成功建立。

### 原因四：后端数据库连接环境变量配置缺失
在上述问题都修复后，当真正进行注册时又暴露了一个新问题，即后端代码 `backend/src/config.py` 中并没有正确地从环境中读取并利用完整的 `DATABASE_URL`，而是根据默认值硬拼接，导致 FastAPI 去连接了一个不存在（或不可达）的名为 `postgres` 的数据库，而 docker-compose 里配置的数据库名为 `agent_db`。
通过增加 `DATABASE_URL: str | None = os.getenv("DATABASE_URL")` 并在 `database_url` 的属性中优先返回它，解决了此连接问题。

### 原因五：Passlib 库与 Bcrypt 新版本的兼容性断言问题 (500 Internal Server Error)
最后，虽然数据库正确连接上了，但当 FastAPI 试图使用 `passlib` 处理密码进行 `bcrypt` 盐值加密时，又抛出了以下内部错误：
> `AttributeError: module 'bcrypt' has no attribute '__about__'`
进而引发了：
> `ValueError: password cannot be longer than 72 bytes, truncate manually if necessary (e.g. my_password[:72])`
这其实是由于最新的 `bcrypt` (>= 4.0.0) 库进行了底层接口变动，而当前使用的 `passlib` (1.7.4 版本) 未跟进所导致的经典兼容性 Bug。

**修复操作：**
修改了 `backend/requirements.txt`，固定安装与 `passlib` 兼容的特定版本 `bcrypt==4.0.1`（或者 `bcrypt==3.2.2` 等稳定支持版本，在此选用重新锁定兼容范围），接着重新 Build 后端镜像以安装依赖。
```bash
docker-compose build backend
docker-compose up -d backend
```

**至此，从前端提交到后端哈希加密再到数据库插入的全数据流已经完全畅通无阻，注册功能彻底修复。**

## 3. 辅助调试的改进措施
在排查期间，为了更明确地看到数据流与错误节点，我为代码增加了以下调试信息：
- 在 `backend/src/routes/auth.py` 中记录请求接收及执行进度。
- 在 `frontend/src/views/RegisterView.vue` 控制台打印用户提交动作和具体的返回 Error 结构。
- 在 `frontend/src/api/index.js` Axios 拦截器中记录所有发起与响应的 HTTP 流量细节。
