# 开发记录

> 记录近期开发改动与关键决策，方便回顾与后续接续开发。

## 一、核心目标

项目本质是「**云端文件中转站**」：

```
AstrBot/LLM → MCP write_file → 服务器 data/ 目录 → 前端网页列表 → 任意设备下载
```

围绕这个目标，近期做了：Go MCP 服务器重构、前端重构、文件目录管理、操作存档监控、流式下载、简单鉴权。

---

## 二、Go MCP 服务器（mcp-server-go）

用 Go 重写的 MCP 服务器，独立于 Python 版（`mcp-server/`），位于 `mcp-server-go/`，分支 `go-mcp`。

**技术栈**：`mark3labs/mcp-go`（SSE 传输）+ `pgx`（PostgreSQL）+ `slog`（日志）。

**结构**：

```
mcp-server-go/
├── cmd/server/main.go        # 入口：起 SSE server + 优雅停机
├── config.yml(.example)      # YAML 配置（config.yml 已 gitignore）
└── internal/
    ├── config/               # YAML + env 覆盖
    ├── logging/              # slog 结构化日志
    ├── store/                # pgx 连接池 + operations/commands 表操作
    ├── service/              # SafeWrite / SafeList / SafeModify / ExecuteCommand
    ├── tools/                # 共享 helper（common.go）+ 每个工具一个子包
    │   ├── writefile/        #   write_file
    │   ├── listdatafiles/    #   list_data_files
    │   ├── modifydatafile/   #   modify_data_file
    │   ├── executecommand/   #   execute_command
    │   ├── getresults/       #   get_results
    │   └── segmentedreply/   #   segmented_reply（config.json 用 go:embed 嵌入）
    └── mcp/                  # server 组装 + auth 中间件
```

**已实现工具（6 个，全部移植自 Python 版）**：`write_file`、`list_data_files`、`modify_data_file`、`execute_command`、`get_results`、`segmented_reply`。每个工具一个子包，共享 helper 收在 `internal/tools/common.go`，子包只 import 父包，无循环依赖。

**关键设计**：
- 配置文件走 YAML + env 覆盖（`MCP_PORT` / `MCP_BASE_URL` / `DATA_DIR` / `DATABASE_URL` / `MCP_API_KEY` / `LOG_LEVEL` / `LOG_FORMAT`）。
- **日志 vs 存档分离**：技术日志用 `slog`（JSON 到 stdout）；业务存档写入 `operations` 表（`session_id` 从 MCP 会话上下文取，同一连接自动归组）。
- `operations` 表启动时 `CREATE TABLE IF NOT EXISTS` 自动建，无需手动迁移。
- 鉴权：`api_key` 留空 = 不启用，填了则强制 `Authorization: Bearer <key>`。

---

## 三、后端（backend，Python FastAPI）

### 1. 文件目录树
- `files.py` 改为**递归目录树**返回（`{tree: [{name, path, type, children, size, modified_at}]}`），不再扁平。
- 下载接口支持子目录：`GET /api/files/download?path=sub/foo.txt`。
- `data_dir` 可配置（`config.yml` → `data_dir`，或环境变量 `DATA_DIR`），修复了原硬编码 `/app/data` 导致本地读不到文件的问题。

### 2. 操作存档接口
- 新增 `Operation` 模型 + `GET /api/operations`（分页），读 Go MCP 写入的 `operations` 表。
- `database/init.sql` 补了 `operations` 表定义。

### 3. 下载鉴权（共享密钥）
- 下载接口校验 `ACCESS_TOKEN`（`config.yml` → `access_token`，或环境变量 `ACCESS_TOKEN`），用 `hmac.compare_digest` 常数时间比较。
- **留空 = 不鉴权**（当前开发阶段默认），部署时填一个随机长串即可启用。
- 权衡：流式下载带不了 header，token 走 URL query 参数，会出现在服务器访问日志（个人服务器可接受）。

---

## 四、前端（frontend，Vue 3 + Vite）

### 重构要点
- **组件抽取**：`AppLayout`（导航+布局）、`StatusBadge`（状态徽章）、`FileTreeNode`（递归目录树节点）。
- **工具抽取**：`utils/format.js`、`utils/download.js`、`utils/jwt.js`、`utils/token.js`。
- **api 层**：去掉全部 `[DEBUG]` 日志，token 刷新逻辑。
- **auth store**：解析 JWT 拿 username + 过期判断（原写死 `{username:'user'}`）。
- **路由**：嵌套路由（`AppLayout` 包 `files`/`dashboard`），默认进 `/files`。
- 移除了废弃的 `RegisterView.vue`、`CommandDetail.vue`（移到 `旧内容/`）。

### 页面
- **Files 页**：目录树浏览 + 下载（递归组件渲染，可折叠）。
- **Activity 页**（原 Commands 页）：显示操作存档（时间 / 工具 / 文件 / 状态），分页，用于监控「别人用服务器做了什么」。

### 下载方式（流式）
前端不再用 `axios blob`（占内存），改成直接跳转下载链接，浏览器原生流式下载：

```js
window.location.href = `/api/files/download?path=${encodeURIComponent(path)}&token=...`;
```

### 依赖与构建
- `package.json` 版本对齐（`vue ^3.5.13` + `pinia ^2.3.1`），解决 peer 冲突。
- 新增 `pnpm-workspace.yaml`，授权 `esbuild` / `vue-demi` 跑构建脚本（pnpm 11 默认拦截 postinstall）。

---

## 五、鉴权状态（开发阶段）

- **后端**：`files.py` / `operations.py` / `commands.py` 均已去掉 `Depends(get_current_user)`，接口无需登录。
- **前端**：路由守卫移除，默认直接进 Files 页，不再跳登录。
- 登录页 / `auth store` / `api/auth.js` / 登录接口**保留未删**，恢复时把 `Depends(get_current_user)` 加回 + 前端守卫加回即可（git 历史都有）。
- 上线前安全底线：**要么恢复鉴权，要么填 `ACCESS_TOKEN`**，否则暴露端口即可被任意下载 `data/` 文件。

---

## 六、本地启动

### 后端（`start-backend.ps1`，PowerShell 7）
```powershell
pwsh -File start-backend.ps1
```
依次：装依赖 → 起/复用 `agent_postgres`（端口 54322，自动建表）→ 起后端 `http://localhost:18000`。
脚本用环境变量覆盖配置，无需改 `config.yml`（数据库指向 `localhost:54322`，数据目录指向项目根 `data/`）。

### 前端
```bash
cd frontend
pnpm install
pnpm dev   # http://localhost:5173，自动代理 /api 到 18000
```

### 依赖安装备注
- 后端缺 `asyncpg` / `passlib` / `bcrypt` 时，`pip install -r backend/requirements.txt -i https://mirrors.aliyun.com/pypi/simple/`。
- 前端 pnpm 若报 SSL/peer 错误，先确认 `pnpm-workspace.yaml` 存在，网络抖动重试即可。

---

## 七、开发规范（skill）

新增全局 skill **`use-powershell-7`**（`~/.config/opencode/skills/use-powershell-7/SKILL.md`）：

- 所有 shell 命令/脚本一律用本地 PowerShell 7（`C:\Program Files\PowerShell\7\pwsh.exe`）。
- 禁止 `.bat` / `.cmd`，脚本统一 `.ps1`。
- 约定：`$PSScriptRoot`、`$LASTEXITCODE`、`*> $null`、反引号续行、`$env:NAME`。

---

## 八、待办

- [ ] **批量下载**：选中多个文件 → 打包 zip → 下载（规划见 `批量下载规划.md`）。
- [ ] Go MCP 写 Dockerfile 并接入 docker-compose（挂载 `./data:/app/data`，统一 `/app/data`）。
- [ ] 文件管理：新建文件夹 / 移动 / 重命名（目前只做了目录树展示 + 子目录下载）。
- [ ] 部署时确定鉴权方案（恢复 JWT 或启用 `ACCESS_TOKEN`）。
- [ ] 共享密钥的 URL token 若在意日志泄漏，可升级为签名 URL（签名 + 过期时间）。
