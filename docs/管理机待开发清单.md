# 管理机待开发清单（manager-go）

> 写于 2026-09-11。按优先级从上往下做，完成一项把 `[ ]` 改 `[x]`。
> **轻量化口径（硬约束）**：不引入 ORM / 迁移框架；优先复用 `file_metadata`
> 既有列（copied_from / moved_from / missing_rounds / checksum / scope），
> 能不加表就不加表。确需新列/新表时，在该条目里**显式标注并说明理由**，
> 由人类确认后再动。
> 依赖方向铁律不变：`HTTP/MCP → manager → repo + describer + indexer`，
> manager 不 import 装配层与兄弟模块（IndexSink 走结构接口）。

## 0. 现状速览

| 域 | 文件 | 状态 |
|---|---|---|
| 入库 intake | intake.go | ✅ Write/Modify/Move/LogicTree/LogicPaths + uuid 派生物理随机路径 |
| 取件 fetch | fetch.go | ✅ Locate/LocateMany/Open/OpenByLogic/Read/ReadByLogic/StreamByLogic + 三哨兵 |
| 缓冲 buffer | buffer.go | ✅ LRU 快照 + stat 新鲜度 + 大文件旁路 |
| 更新回填 updater | updater.go | ✅ T2 Backfill + T3 AnalyzeFile + 幽灵 3 轮软删 + 喂索引 |
| 位置 placement | placement.go | ⚠️ `resolve` 在用；`Resolve()` 是死 stub（fetch.Locate 已覆盖，待收口） |
| 谱系 lineage | lineage.go | ❌ `Related()` 死 stub（copied_from/moved_from 数据已在） |
| 对账 audit | audit.go | ❌ `Audit()` 死 stub（幽灵计数已被 updater 的 missing_rounds 代做） |
| 下载票据 download | download.go | ❌ 死 stub；真实现漂移到 `mcp-server-go/internal/service/ticket.go` |

## P0 清理与校准（无 DB 变更）

- [x] **P0-1 placement.Resolve 收口**：实现为 `fetch.Locate` 的薄壳（uuid → 逻辑路径），
  退役 `errNotPlaced` 死 stub；或明确标注"由 Locate 承接"。落点 `placement.go`。
  ✅ 2026-09-11：`Resolve` = `Locate(uuid).Path`，`errNotPlaced` 退役；`placement_test.go` 覆盖。
- [x] **P0-2 fetch 逻辑口回执补全**：`locateByLogic` 只填 UUID/Path/IsDeleted，
  缺 Scope/MimeType/SizeBytes（uuid 口有）——同一文件两口径。落点：
  `MetaRow` 扩 Scope/MimeType 两字段 + `manager_adapter.go` `toMetaRow` 填 +
  `fetch.go` `locateByLogic` 组回执。
  ✅ 2026-09-11：三字段补全，`placement_test.go` `TestOpenByLogicReceipt` 断言。
- [x] **P0-3 文档漂移校正**：
  - `docs/管理机开发交接.md` §3 说 manager require indexer-go——实际 go.mod 没有
    （IndexSink 结构接口，零依赖），改。
  - `docs/管理机开发交接.md` §2 状态表与现状对齐（placement Resolve / download 漂移）。
  - `docs/架构设计.md` §4 管理机状态表校准。
  ✅ 2026-09-11 全部校正。

## P1 谱系域 lineage（复用既有列，无新表）

- [ ] **P1-1 `Related(uuid)` 实现**：出边取 `copied_from`（copied）+ `moved_from`（moved）；
  入边反查"谁复制自我"（`WHERE copied_from = path`）。
- [ ] **P1-2 Store 面扩展**：`ReverseCopiedFrom(ctx, path) ([]MetaRow, error)`（按既有列反查，无新表）。
- [ ] **P1-3 暴露**：文件详情已有 moved_from；补 MCP/HTTP 关系查询入口（可选）。
- 验收：复制/移动后 `Related` 能给出双向邻居；L1 用 memStore 断言。

## P2 对账域 audit（只报告不动手；无新表）

- [ ] **P2-1 孤儿**：枚举 dataDir 下物理文件（`<uuid前2>/<uuid><ext>` 派生位），
  解析 uuid → DB 批量存在性查询 → 盘有 DB 无 = 孤儿。落点：manager 盘枚举 +
  `Store.UUIDsExist(ctx, []string) (map[string]bool, error)`。
- [ ] **P2-2 幽灵**：读 `file_metadata.missing_rounds`（既有列）出报告（带连续缺失轮次）；
  动作仍是 updater 的 3 轮软删，本域只报告。
- [ ] **P2-3 重复 checksum**：`Store.DupChecksumGroups(ctx) ([]DupGroup, error)`，SQL `GROUP BY checksum HAVING count(*)>1`。
- [ ] **P2-4 暴露**：MCP 工具（如 `audit_files`）+ HTTP `GET /api/admin/audit`（admin 面）。
- 验收：造一个绕过 intake 的盘上文件（孤儿）、一个盘上删除的幽灵行、两份同内容文件，报告三类齐全。

## P3 download 域归位（消除漂移，安全敏感）

- [x] **P3-1 决策：全归 manager（方案 A）**：`manager-go/download.go` 实现
  `IssueDownloadURL(path, uuid, ttl)`（含 BaseURL 拼装）+ `VerifyDownloadTicket`；
  `DownloadConfig{Secret, BaseURL}` 由装配层从 config 解析后经 `manager.New` 注入。
  ✅ 2026-09-11：`service/ticket.go` + `ticket_test.go` 移入 `旧内容/下载票据归位-20260911/`；
  `tools/common.go` 的 `DownloadURL` 退役（writefile 改调 `deps.Manager.IssueDownloadURL`）；
  `api/files.go` 验签改调 `s.manager.VerifyDownloadTicket`。
- [x] **P3-2 迁移单测**：`manager-go/download_test.go`（往返/单文件绑定/换 uuid/篡改/过期/转义/未启用）；
  `tools/common_test.go` 下载用例随迁（保留键空间用例）。
  ✅ 2026-09-11。
- 附带修复（本批发现的真 bug）：`core/search.go` 分页在排序之前的时序错误——
  `order_by` + `size=1` 取不到极值；改为排序先于分页。

## P4 Store 接口扩展（随 P1/P2 增，集中记录）

- [ ] `ReverseCopiedFrom` / `UUIDsExist` / `DupChecksumGroups` / `MissingRounds` 读——
  全部走既有表既有列，**不新增表**；确需新列时单独开条目评审。

## 可选（低优先）

- [ ] Manager 各写口补齐日志五要素（session/duration/结果计量）。
- [ ] `LogicTree` 全表扫描按请求重复——量大再考虑缓存。
